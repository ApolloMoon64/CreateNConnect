MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=createnconnect
APP_BASE_URL=http://localhost:3000

# If your local MySQL uses a socket instead of TCP, set this and leave host/port unused.
# MYSQL_SOCKET_PATH=/usr/local/mysql/mysql.sock

# Email delivery for meeting invites and password resets
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
EMAIL_FROM=
MEETING_EMAIL_FROM=
